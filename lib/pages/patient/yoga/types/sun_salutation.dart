import 'package:flutter/material.dart';

class SunSalutation extends StatefulWidget {
  const SunSalutation({Key key}) : super(key: key);

  @override
  _SunSalutationState createState() => _SunSalutationState();
}

class _SunSalutationState extends State<SunSalutation> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
